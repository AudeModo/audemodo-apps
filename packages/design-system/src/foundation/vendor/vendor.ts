/**
 * 감싸고 있는 벤더의 이름.
 *
 * 이 값이 필요한 곳은 벤더가 무엇인지 **보여주는 것이 목적인 화면**이다. 앱이 이 문자열을
 * 직접 적으면 저장소 전체 grep에 앱 소스가 걸려 격리 검사가 무뎌진다 — 검사가 잡아야
 * 하는 것은 「앱이 벤더를 부르는가」인데, 화면에 이름을 적은 것과 구별되지 않는다.
 *
 * 그래서 이름은 경계 안쪽인 이 패키지가 들고 있고 앱은 값으로 받아 간다.
 */
export const VENDOR_NAME = '@astryxdesign';

/**
 * 한 번에 보여줄 편수. 초기 노출이자 「더 보기」의 증분이다.
 *
 * 서버가 처음 그리는 목록과 브라우저가 이어받은 뒤의 목록이 같은 수를 써야 한다.
 * 두 곳이 다르면 이어받는 순간 목록의 길이가 달라진다.
 *
 * 이 값이 훅이 아니라 여기 있는 이유: 훅은 클라이언트 모듈이고, 클라이언트 모듈의
 * 값을 서버 컴포넌트에서 import하면 실제 값이 아니라 참조가 넘어온다. 숫자로 쓰면
 * 조용히 NaN이 되고 아무것도 그려지지 않는다 — 타입 검사에도 걸리지 않는다.
 */
export const POSTS_PER_PAGE = 10;
